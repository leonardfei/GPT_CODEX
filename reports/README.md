# Reports

Each executed task produces one structured Markdown report named `task_XXX_report.md`.

Reports separate observed quantitative evidence, QC findings, computational interpretation, and biological hypotheses. They also record inputs, outputs, scripts, commands, parameters, environment, deviations, and Git state.

Use `templates/report_template.md` for new task reports.

# Figures

Store reviewable figures generated by tasks here, using names such as `task001_overview.pdf`.

Every figure should have a documented generating script, input provenance, parameters, and QC notes in the matching report.

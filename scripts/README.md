# Scripts

Executable implementation code belongs here and should be named with the matching three-digit task identifier.

- `R/` for R scripts;
- `python/` for Python scripts;
- `shell/` for shell scripts.

Scripts must not silently change scientific definitions. Record the exact command, inputs, outputs, parameters, environment, and warnings in the matching report.
